name: DOCS

on:
  push:
    branches: ["main"]
    paths:
      - '**.md'
      - 'documents/**'
      - 'media/**'
      - 'design/**'
      - 'info.yaml'
      - 'config.yaml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "docs"
  cancel-in-progress: true

jobs:
  docs:
    uses: analogicus/jnw-actions/.github/workflows/docs.yaml@370e41ee09833cc320121b0d3762c5eab43d63b3
    permissions:
      contents: read
      pages: write
      id-token: write
